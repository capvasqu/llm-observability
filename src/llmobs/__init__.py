"""llmobs — a local, decoupled LLM observability layer.

Measures cost, tokens, latency and quality for any project that calls an LLM,
without coupling to that project's business logic or language.
"""

__version__ = "0.1.0"
